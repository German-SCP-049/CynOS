{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch QEMU",
            "type": "shell",
            "linux": {
                "command": "qemu-system-x86_64 -s -S -drive format=raw,file=./bin/CynOS.bin -daemonize"
            }
        }
    ]
}